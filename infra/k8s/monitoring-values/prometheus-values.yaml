# Optimized values for kube-prometheus-stack on Minikube
prometheus:
  prometheusSpec:
    resources:
      requests:
        memory: "256Mi"
        cpu: "100m"
      limits:
        memory: "512Mi"
        cpu: "250m"
    storageSpec:
      volumeClaimTemplate:
        spec:
          accessModes: ["ReadWriteOnce"]
          resources:
            requests:
              storage: 2Gi
    externalUrl: http://medinsightpds.duckdns.org/prometheus
    routePrefix: /prometheus
  ingress:
    enabled: true
    ingressClassName: nginx
    hosts:
      - medinsightpds.duckdns.org
    paths:
      - /prometheus
    pathType: Prefix

grafana:
  enabled: true
  adminPassword: admin
  ingress:
    enabled: true
    ingressClassName: nginx
    hosts:
      - medinsightpds.duckdns.org
    path: /grafana
    pathType: Prefix
  grafana.ini:
    server:
      root_url: http://medinsightpds.duckdns.org/grafana/
      serve_from_sub_path: true
  serviceMonitor:
    enabled: true
    path: /grafana/metrics
  sidecar:
    dashboards:
      enabled: true
      label: grafana_dashboard
      searchNamespace: ALL
  additionalDataSources:
    - name: Loki
      type: loki
      url: http://loki:3100
      access: proxy
      isDefault: false
      jsonData:
        maxLines: 1000

alertmanager:
  enabled: false

prometheusOperator:
  resources:
    limits:
      cpu: 200m
      memory: 200Mi
    requests:
      cpu: 100m
      memory: 100Mi

nodeExporter:
  enabled: true

kubeStateMetrics:
  enabled: true

kubeControllerManager:
  enabled: false
kubeScheduler:
  enabled: false
kubeEtcd:
  enabled: false